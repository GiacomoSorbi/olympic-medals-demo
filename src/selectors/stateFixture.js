import { olympicMedalsByCountry as medalsData } from '../fixtures/data'

const stateFixture = {
  interface: {
    openModal: 'pippiLand',
  },
  medalsData: {
    medalsData,
  },
}

export default stateFixture
