import { olympicMedalsByCountry as medalsData } from '../fixtures/data'

const stateFixture = {
  interface: {
    openModal: 'pippiLand',
    sortCriteria: 'silver-desc',
  },
  medalsData: {
    medalsData,
  },
}

export default stateFixture
